package main

// Version is the current NodeGUI door release (semver).
// Bump the patch (x.x.N) on every change; bump minor for significant features;
// bump major only on explicit request.
const Version = "0.3.0"
